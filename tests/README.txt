Instrumentation tests for QuantraVision Overlay.
These confirm that static detection runs deterministically offline
and logs provenance entries in the local Room database.
Run via Android Studio > Run 'All Tests' or Gradle `connectedCheck`.
